import 'package:flutter/material.dart';

class MangaChapters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(30),
      child: Text('Manga Chapters Here'),
    );
  }
}
